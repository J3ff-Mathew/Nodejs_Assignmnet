module.export = {
    add = (a, b) => console.log(`Addition:  ${a + b}`),
    sub = (a, b) => console.log(`Subtraction:  ${a - b}`),
    mult = (a, b) => console.log(`"Multiplication:"  ${a * b}`),
    div = (a, b) => console.log(`"Division:"  ${a / b}`),
}